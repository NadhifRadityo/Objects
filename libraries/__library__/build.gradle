plugins {
    id 'java'
    id 'org.jetbrains.kotlin.jvm' version '1.5.30'
}

buildDir = '../__target__/__sources__/'

context(this) { require('libraryModules.gradle') }
def module = ext_libraryModules_module
module('GameLibrary - LWJGL')

context(this) { require('library.gradle') }
