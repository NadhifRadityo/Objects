plugins {
    id 'java'
}

apply from: 'common.gradle'
require(this, 'common.gradle')

repositories {
    mavenCentral()
}

dependencies {
    implementation project('Packages$Functional')
}

$_.forwardTask('Library', { it.group == 'library' })
