plugins {
    id 'java'
}

apply from: 'common.gradle'

repositories {
    mavenCentral()
}

dependencies {
    implementation project('Packages$Functional')
}

ext_common_forward_task('Library', { it.group == 'library' })
